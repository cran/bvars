% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_shocks.R
\name{compute_shocks.PosteriorBVAR}
\alias{compute_shocks.PosteriorBVAR}
\title{Computes posterior draws of  shocks}
\usage{
\method{compute_shocks}{PosteriorBVAR}(posterior)
}
\arguments{
\item{posterior}{posterior estimation outcome - an object of class 
\code{PosteriorBVAR} obtained by running the \code{estimate} function.}
}
\value{
An object of class \code{PosteriorShocks}, that is, an \code{NxTxS} 
array with attribute \code{PosteriorShocks} containing \code{S} draws of the shocks.
}
\description{
Each of the draws from the posterior estimation of the BAVR 
model is transformed into a draw from the posterior distribution of the shocks.
}
\examples{
# simple workflow
############################################################
spec = specify_bvar$new(us_macro_chan)        # specify the model
burn = estimate(spec, 5)                      # run the burn-in
post = estimate(burn, 10)                     # estimate the model
shoc = compute_shocks(post)                   # compute shocks

# workflow with the pipe |>
############################################################
us_macro_chan |>
  specify_bvar$new() |>
  estimate(S = 5) |> 
  estimate(S = 10) |> 
  compute_shocks() -> shoc
  
}
\author{
Tomasz Woźniak \email{wozniak.tom@pm.me}
}
